#!/bin/sh
# Block pushing to main. Use a PR workflow instead.
while read -r local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "❌ Pushing to main is disabled. Create a branch and open a PR instead."
    exit 1
  fi
done
exit 0
